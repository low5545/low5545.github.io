source 'https://rubygems.org'
group :jekyll_plugins do
    # gem 'github-pages' # This prevents local plugins from importing.
                         # If this gem is required, build a gem for the
                         # local plugin and include below and _config.yml.
                         # Reference: https://github.com/github/pages-gem/issues/283
    gem 'jekyll'
    gem 'kramdown-parser-gfm'
    # gem 'jekyll-email-protect'
    gem 'jekyll-github-metadata'
    gem 'jekyll-paginate-v2'
    gem 'jekyll-scholar'
    gem 'jekyll-twitter-plugin'
    gem 'jemoji'
    gem 'unicode_utils'
end
